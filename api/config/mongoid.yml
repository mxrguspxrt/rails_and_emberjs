production:
  sessions:
    default:
      database: api_production
      hosts:
        - localhost:27017
      options:
  options:

development:
  sessions:
    default:
      database: api_development
      hosts:
        - localhost:27017
      options:
  options:

test:
  sessions:
    default:
      database: api_test
      hosts:
        - localhost:27017
      options:
        read: primary
        max_retries: 1
        retry_interval: 0
